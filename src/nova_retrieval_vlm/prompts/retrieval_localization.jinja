IMPORTANT: Return **valid JSON only** matching this exact schema – no markdown, no commentary:
{
  "image_id": "<string | int>",
  "boxes": [[x1, y1, x2, y2], ...],
  "labels": ["label1", ...],
  "scores": [0.9, ...]
}
You are an expert neuroradiology assistant.
Analyze the following MRI image (dimensions: {{ width }}×{{ height }} pixels) to identify and localize all abnormalities.

Image: {{ img_path }}

Here are source guideline excerpts that may help:
{% for p in passages %}
- {{ p }}
{% endfor %}

#  Bounding boxes MUST be absolute pixel coordinates as
#  [x1, y1, x2, y2] (top-left to bottom-right). Ensure x2 > x1 and y2 > y1.
#  Do NOT output [x, y, width, height]. 

Image coordinate system:
• Origin (0, 0) is the top-left corner.
• Bottom-right corner is ({{ width | int - 1 }}, {{ height | int - 1 }}).
• All x coordinates must be in [0, {{ width | int - 1 }}]; y in [0, {{ height | int - 1 }}].

Response rules (STRICT):
1. Output must be valid JSON **only** – no markdown fences, no explanations, no extra keys.
2. Provide at most **3** bounding boxes. Skip detections with confidence < 0.05.
3. Use exactly the label string "anomaly" for every box (single-class).
4. If no abnormality is present, set boxes, labels and scores to empty lists.
5. Boxes MUST follow the [x1, y1, x2, y2] format (x1 < x2, y1 < y2). Width/height format is INVALID.

Example:
{
  "image_id": {{ image_id }},
  "boxes": [[50, 30, 120, 180]],
  "labels": ["anomaly"],
  "scores": [0.91]
}

Return ONLY the JSON. 