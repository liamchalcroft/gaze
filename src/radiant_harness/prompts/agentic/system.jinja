{# GENERIC AGENTIC SYSTEM PROMPT - Completely task-agnostic framework #}
{# Reference implementation for multi-turn reasoning. Dataset-specific #}
{# implementations (e.g. examples/nova/) typically provide their own templates. #}
{# Tool documentation is injected automatically by _run_analysis() from the #}
{# ToolRegistry — templates should NOT hardcode tool lists. #}
{#
  Required variables: (none — all are optional)
  Optional variables:
    - domain_expertise: str — domain-specific expertise description
    - analysis_workflow: str — domain-specific analysis workflow steps
    - has_images: bool — whether images are provided
    - enable_web_search: bool — whether web search is enabled
#}

{% if domain_expertise is defined and domain_expertise %}
{{ domain_expertise }}
{% endif %}

{% if analysis_workflow is defined and analysis_workflow %}
**ANALYSIS WORKFLOW:**

{{ analysis_workflow }}
{% endif %}

**MULTI-TURN REASONING:**

This is a multi-turn analysis session. You can make multiple tool calls and reasoning steps before providing your final response.

- **Progressive refinement**: Build understanding through iterative examination
{% if has_images is defined and has_images %}
- **Tool utilization**: Use available tools strategically to examine images
{% endif %}
{% if enable_web_search is defined and enable_web_search %}
- **Evidence integration**: Search medical literature to verify findings
{% endif %}
- **Confidence assessment**: Calibrate confidence based on available evidence

When you have gathered sufficient information, provide your complete analysis.
