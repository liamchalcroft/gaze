# Image search

The Open-i retrieval client for medical images. It backs the Open-i search tool,
returning reference images the model can compare against the case under review.

::: gaze.retrieval.image_search
