# OpenAI adapter

`OpenAIAdapter` targets the OpenAI API and any OpenAI-compatible service such as
OpenRouter, with retries and `response_format` support. For how to supply it via
the `adapter_factory` pattern, see [Getting started](../../getting-started.md).

::: gaze.models.openai_adapter
